# don't ever lint node_modules
node_modules
# don't lint build output (make sure it's set to your correct build folder name)
dist
# don't lint nyc coverage output
coverage
# Makes those annoying module.exports errors go away.
.eslintrc.js
# The jest config has to be in javascript
jest.config.js
