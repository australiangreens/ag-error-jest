{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "module": "commonjs",
    "target": "es2020",
    "outDir": "dist/cjs",
    // We don't need the declaration files in both places, they are basically
    // just for editor hinting. As per
    // https://www.typescriptlang.org/docs/handbook/declaration-files/templates/module-d-ts.html
    // "The .d.ts syntax intentionally looks like ES Modules syntax"
    // So it makes more sense to put the declarations in the esm directory
    "declaration": false
  }
}
